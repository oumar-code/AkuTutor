name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: aku
          POSTGRES_PASSWORD: aku
          POSTGRES_DB: aku_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
<<<<<<< Updated upstream
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Lint (Ruff)
        run: ruff check .

      - name: Format check (Black)
        run: black --check .

      - name: Run tests
        env:
          DATABASE_URL: postgresql+asyncpg://aku:aku@localhost:5432/aku_test
          JWT_SECRET_KEY: test-secret-key
        run: pytest --cov=app --cov-report=xml -v

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t aku/akututor:${{ github.sha }} .
=======
          node-version: '20'
      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - run: npm install
      - run: npm run build || echo "No build step"
      - run: npm test
      - name: Lint
        run: npm run lint || echo "No lint script yet"
      - name: Build Docker image
        run: docker build -t akututor:ci .
      - name: Build docs (placeholder)
        run: echo "No docs build step yet"
>>>>>>> Stashed changes
